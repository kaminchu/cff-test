const fixture = JSON.parse(globalThis.__cff_kvs_json); const associated = globalThis.__cff_kvs_associated;
function convert(item, format) { const requested = format || "string"; if (["string", "json", "bytes"].indexOf(requested) < 0) throw new TypeError("format must be string, json, or bytes"); if (item.format === requested) { if (requested === "bytes") return Buffer.from(item.value, "base64"); return requested === "json" ? JSON.parse(JSON.stringify(item.value)) : item.value; } if (item.format === "string" && requested === "json") return JSON.parse(item.value); if (item.format === "string" && requested === "bytes") return Buffer.from(item.value, "utf8"); if (item.format === "json" && requested === "string") return JSON.stringify(item.value); if (item.format === "bytes" && requested === "string") return Buffer.from(item.value, "base64").toString("utf8"); throw new TypeError("value cannot be converted to the requested format"); }
const handle = Object.freeze({ async exists(key) { return !!(fixture && fixture.values && Object.prototype.hasOwnProperty.call(fixture.values, key)); }, async get(key, options) { if (!fixture || !fixture.values || !Object.prototype.hasOwnProperty.call(fixture.values, key)) throw new Error("key does not exist: " + key); return convert(fixture.values[key], options && options.format); }, async meta() { if (!fixture) throw new Error("KVS is not associated"); return JSON.parse(JSON.stringify(fixture.meta)); } });
const cloudfront = Object.freeze({ kvs() { if (!associated) throw new Error("KVS is not associated"); return handle; }, updateRequestOrigin() {} }); globalThis.__cff_cloudfront = cloudfront; export default cloudfront;
