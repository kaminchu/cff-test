function escape(value) { return encodeURIComponent(String(value)); } function unescape(value) { const input = String(value); return input.replace(/(?:%[0-9a-fA-F]{2})+/g, (part) => { const bytes = []; for (let index = 0; index < part.length; index += 3) bytes.push(parseInt(part.slice(index + 1, index + 3), 16)); return new TextDecoder().decode(new Uint8Array(bytes)); }); }
function parse(value, separator, equal, options) { const result = {}; const source = String(value); const sep = separator === undefined ? "&" : String(separator); const eq = equal === undefined ? "=" : String(equal); const settings = options || {}; const maxKeys = settings.maxKeys === undefined ? 1000 : Number(settings.maxKeys); if (!source) return result; const parts = source.split(sep); let count = 0; const decoder = typeof settings.decodeURIComponent === "function" ? settings.decodeURIComponent : (item) => unescape(item); for (let index = 0; index < parts.length && (maxKeys === 0 || count < maxKeys); index++) { const part = parts[index]; const position = part.indexOf(eq); const rawKey = position < 0 ? part : part.slice(0, position); const rawValue = position < 0 ? "" : part.slice(position + eq.length); const key = decoder(rawKey.replace(/\+/g, " ")); const item = decoder(rawValue.replace(/\+/g, " ")); if (Object.prototype.hasOwnProperty.call(result, key)) result[key] = Array.isArray(result[key]) ? result[key].concat([item]) : [result[key], item]; else result[key] = item; count++; } return result; }
function stringify(object, separator, equal, options) { const sep = separator === undefined ? "&" : String(separator); const eq = equal === undefined ? "=" : String(equal); const settings = options || {}; const encoder = typeof settings.encodeURIComponent === "function" ? settings.encodeURIComponent : escape; const output = []; Object.keys(Object(object)).forEach((key) => { const value = object[key]; const values = Array.isArray(value) ? value : [value]; values.forEach((item) => output.push(encoder(key) + eq + (item === null || item === undefined ? "" : encoder(item)))); }); return output.join(sep); }
const querystring = Object.freeze({ escape, unescape, parse, decode: parse, stringify, encode: stringify }); globalThis.__cff_querystring = querystring; export { escape, unescape, parse, stringify }; export const decode = parse; export const encode = stringify; export default querystring;
