(() => { const staticMembers = ["alloc", "allocUnsafe", "byteLength", "compare", "concat", "from", "isBuffer", "isEncoding"]; const prototypeMembers = ["compare", "copy", "equals", "fill", "includes", "indexOf", "lastIndexOf", "readInt8", "readIntBE", "readInt16BE", "readInt32BE", "readIntLE", "readInt16LE", "readInt32LE", "readUInt8", "readUIntBE", "readUInt16BE", "readUInt32BE", "readUIntLE", "readUInt16LE", "readUInt32LE", "readDoubleBE", "readDoubleLE", "readFloatBE", "readFloatLE", "subarray", "swap16", "swap32", "swap64", "toJSON", "toString", "write", "writeInt8", "writeIntBE", "writeInt16BE", "writeInt32BE", "writeIntLE", "writeInt16LE", "writeInt32LE", "writeUInt8", "writeUIntBE", "writeUInt16BE", "writeUInt32BE", "writeUIntLE", "writeUInt16LE", "writeUInt32LE", "writeDoubleBE", "writeDoubleLE", "writeFloatBE", "writeFloatLE", "buffer", "byteOffset", "length"]; Object.getOwnPropertyNames(Buffer).forEach((name) => { if (staticMembers.indexOf(name) < 0 && name !== "length" && name !== "name" && name !== "prototype") { try { delete Buffer[name]; } catch (_) {} } }); Object.getOwnPropertyNames(Buffer.prototype).forEach((name) => { if (prototypeMembers.indexOf(name) < 0 && name !== "constructor") { try { delete Buffer.prototype[name]; } catch (_) {} } }); })();

const nativeBufferFrom = Buffer.from; const nativeBufferToString = Buffer.prototype.toString;
Buffer.from = function (value, encoding) { if (encoding === "base64url") { const text = String(value).replace(/-/g, "+").replace(/_/g, "/"); return nativeBufferFrom(text + "===".slice((text.length + 3) % 4), "base64"); } return arguments.length < 2 ? nativeBufferFrom(value) : nativeBufferFrom(value, encoding); };
Buffer.prototype.toString = function (encoding, start, end) { if (encoding === "base64url") { const encoded = arguments.length < 2 ? nativeBufferToString.call(this, "base64") : nativeBufferToString.call(this, "base64", start, end); return encoded.replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, ""); } return nativeBufferToString.apply(this, arguments); };
Object.freeze(Buffer.prototype); Object.freeze(Buffer);
